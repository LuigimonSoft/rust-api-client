# rust-api-client
A reusable REST client in Rust that authenticates via form-url-encoded POST to obtain a token and performs GET, POST, and PUT requests with unified handling. Supports JSON bodies, custom headers, and automatic Bearer authentication, making integration with external APIs simple and efficient.
